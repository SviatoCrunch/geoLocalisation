"""Local, git-ignored copy of the ORIGINAL SegProp primitives, used ONLY to prove
numerical parity of our port (``keyframe_calib.segprop``) in the tests.

Source: https://github.com/vlicaret/segprop (Marcu et al., ACCV 2020). The upstream
repo ships NO license, so the code is NOT redistributed in this repository — run
``python -m keyframe_calib._segprop_ref.fetch`` (or let the parity test do it) to
download util.py / flow.py / map2d.py / classmap.py here for local verification.
"""
